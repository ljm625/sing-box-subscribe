[project]
name = "sing-box-subscribe"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "Flask==2.3.2",
    "requests",
    "paramiko",
    "scp",
    "chardet",
    "PyYAML",
    "ruamel.yaml",
]

[tool.vercel]
entrypoint = "api.app:app"