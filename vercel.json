{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "uv pip install -r requirements.txt",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/api/app"
      }
    ]
  }